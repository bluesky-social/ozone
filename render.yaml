services:
  - type: web
    name: ozone-staging
    env: node
    repo: https://github.com/bluesky-social/ozone
    buildCommand: yarn && yarn build
    startCommand: yarn start
    healthCheckPath: /
    plan: starter
    numInstances: 1
    domains:
      - admin.staging.bsky.dev
